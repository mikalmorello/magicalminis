# GitHub Pages Deployment

## Deploy:

1. **In GitHub:** Settings → Pages → Source: **GitHub Actions**

2. **Push to main:**
```bash
git add .
git commit -m "Your message"
git push
```

The workflow automatically builds and deploys on every push to `main`.

Site: `https://magicalminishop.com`


